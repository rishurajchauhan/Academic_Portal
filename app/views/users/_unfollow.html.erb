<%= form_for(current_user.relationships.find_by_followed_id(@user),
             html: { method: :delete },
             remote: false) do |f| %>
  <%= f.submit "Leave Class", :class => "ab" %>
<% end %>